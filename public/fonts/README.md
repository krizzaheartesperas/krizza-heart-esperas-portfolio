Place the Maya/PayMaya brand font here:

- `Tuka.woff2`

`src/styles/globals.css` registers an `@font-face` for `font-family: \"Tuka\"` that points to `/fonts/Tuka.woff2`.
If you don’t add the file (or don’t have a license for it), the UI will fall back to the app’s default sans-serif font.

